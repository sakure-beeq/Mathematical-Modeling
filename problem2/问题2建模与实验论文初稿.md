# 局部模态缺失条件下的多模态情感分类与强度预测

## 摘要

针对文本、语音和视觉特征在局部时间段内缺失的情感预测问题，本文使用附件 2 的对齐特征训练三分类与情感强度联合预测模型，并对附件 3 的无标签样本进行推断。首先，依据文本有效位置、模态可用状态和特征提取质量建立三套独立掩码；语音和视觉特征仅利用训练集有效位置估计标准化参数，并将标准化后的单个特征值限制在 [−5,5]。随后，对三种模态分别进行时序编码，利用跨模态注意力构建缺失位置的潜在表示，再作等权模态融合和时间注意力池化。训练时随机遮挡连续时间区间，同时约束缺失输入与完整输入的分类、回归和潜在表示。最终采用随机种子 2028、dropout 为 0.3 的固定门控模型，以第 9 轮参数作为结果。完整输入下，训练集、验证集和测试集的 Accuracy 分别为 0.7493、0.6484、0.6795，Macro F1 分别为 0.7265、0.6303、0.6398，情感强度 MAE 分别为 0.4802、0.6205、0.6435。验证集三模态同时随机遮挡约 30% 有效位置时，五次重复的平均 Macro F1 为 0.6210，MAE 为 0.6263。实验表明，模型在所考察的局部缺失条件下保持了可用的预测能力，但中性情感的识别仍然较弱。

**关键词：**多模态情感识别；局部模态缺失；跨模态注意力；连续块遮挡；异常值处理

## 1 问题定义与数据来源

问题 2 要求在复杂场景下同时给出三类情感及连续情感强度，并考察文本、语音、视觉信息局部缺失时的表现。本文的监督训练数据来自附件 2 的 `aligned_50.pkl`，使用其已有的训练、验证、测试划分，样本数依次为 3395、728、727。附件 3 的对齐版本含 30 条无标签样本，只用于模型推断。模型没有使用问题 1 生成的多模态特征文件；文本表示由附件 2 和附件 3 自带的 `text_bert` 输入同一份本地冻结 BERT 编码器得到。

每条样本最多包含 50 个对齐位置。冻结 BERT 的文本向量为 768 维，语音特征为 74 维，视觉特征为 35 维。类别标签依次为 Negative、Neutral 和 Positive，强度标签范围为 [−3,3]。训练集三类样本分别为 967、758、1670 条，存在类别不均衡。数据准备阶段检查了标签合法性、强度值有限性及取值范围，同时核对不同划分之间的样本 ID 和视频 ID，未把验证集或测试集信息用于标准化参数估计。

## 2 数据预处理与异常值处理

### 2.1 三类掩码及有效观测

对样本、时间位置和模态的组合，分别定义真实位置掩码 $P$、模态可用掩码 $A$ 和质量掩码 $Q$。模型实际可以使用的位置为

$$O_{i,t,m}=P_{i,t,m}\land A_{i,t,m}\land Q_{i,t,m}. \tag{1}$$

$P=0$ 表示填充区，不属于样本内容；$A=0$ 表示该模态在这一位置不可用；$Q=0$ 表示位置属于内容范围，但原始特征未通过基本质量检查。三者分开保存，使自然缺失、特征提取失败和序列填充不会被混为同一种情况。文本位置由 BERT 的 attention mask 确定。语音与视觉使用文本内容对应的位置，排除首尾的 CLS、SEP 特殊标记。附件 3 若出现文本内部空缺，预处理保留首个至最后一个有效标记之间的内容范围，并把内部空缺标为不可用。

### 2.2 数值异常与缺失的判定

语音或视觉某个特征行只有在所有维度均为有限值、且至少一个维度非零时，才视为可观测。普通训练、验证和测试数据中，内容范围内的全零行或含 NaN、Inf 的行被标记为质量不合格，即 $Q=0$。附件 3 是专门提供的模态缺失样本；其中同样的全零或非有限行按数据集语义标记为不可用，即 $A=0$。这些行的数值在缓存中置零，模型通过掩码及跨模态重建处理，不删除整条样本，也不以全局均值直接填充缺失块。这里的判定是特征行层面的保守规则；它不能进一步辨别全零由采集、提取还是人工缺失造成，因而附件 3 单独采用其缺失数据语义。

在训练集，语音有效行 76,817 个，另有 65 个内容位置未通过质量检查，涉及 4 条样本；视觉有效行 72,633 个，另有 4,249 个位置未通过检查，涉及 421 条样本，其中 110 条样本的视觉模态在内容范围内完全不可用。验证集对应的语音和视觉质量不合格位置分别为 46 和 980 个；测试集分别为 0 和 1,024 个。附件 3 的语音、视觉不可用位置分别为 131 和 135 个，各涉及 27 条样本。上述数量是按位置统计，不能直接解释为被删除的样本数。

### 2.3 训练集统计量与极端数值截断

对语音和视觉每一维，仅利用训练集中满足式（1）的特征行计算均值 $\mu_{m,d}$ 和标准差 $\sigma_{m,d}$。若某维标准差小于 $10^{-6}$，将该维分母设为 1，避免数值不稳定。有效元素按下式处理：

$$\widetilde{x}_{i,t,m,d}=\operatorname{clip}\left(\frac{x_{i,t,m,d}-\mu_{m,d}}{\sigma_{m,d}},-5,5\right). \tag{2}$$

验证集、测试集及附件 3 均沿用训练集统计量。未通过掩码检查的行保持为零，不参与均值和标准差估计。训练集标准化后处于截断边界 $|\widetilde{x}|=5$ 的语音元素为 4,453 个，占语音有效元素约 0.078%；视觉元素为 1,282 个，占约 0.050%。这反映逐维截断触及边界的规模，不等同于独立异常样本的数量。文本不进行这一数值截断，使用冻结 BERT 输出。类别标签未作重标记，强度标签也未作缩尾处理。

## 3 模型构造

### 3.1 模态内时序编码

文本、语音和视觉先各自通过线性投影、LayerNorm 和 GELU 映射到 128 维。每个模态加入时间位置向量和模态标识向量，再经过两层 Transformer Encoder。编码器设 4 个注意力头，前馈层宽度 256，dropout 为 0.3。若位置属于内容范围但模态未观测到，则以可训练的模态缺失向量代替原始投影；真正的填充位置由注意力掩码排除。

### 3.2 跨模态重建与融合

三个模态的时序表示按时间和模态展开，交由一层跨模态 Transformer Encoder 处理。该层同时访问相邻时间位置和其他模态，为每个模态设置独立线性重建头。观测位置保留原时序编码表示，缺失位置使用跨模态重建表示。最终模型配置为 `fixed_gate`：对同一时间位置的**非填充模态槽**赋相等融合权重。因此，原本属于内容范围但观测缺失的模态仍通过重建表示参与融合；不能把这一模型描述为“按预测不确定度动态调整模态权重”。融合结果经时间注意力池化，分别输入三分类头和情感强度回归头。强度头输出 $3\tanh(z)$，使预测值落入 [−3,3]。

数据在模型内的顺序为：预处理后的三模态向量与掩码，单模态投影，缺失向量及位置和模态编码，模态内时序 Transformer，跨模态 Transformer，缺失潜在表示重建，逐位置等权融合，时间注意力池化，分类和回归两个输出头。模型结构见[图 1](release/seed2028/charts/01_model_flow.png)。

### 3.3 连续区间遮挡与联合目标

每个训练批次随机选择文本、语音、视觉及其组合之一，即 T、A、V、TA、TV、AV、TAV；遮挡比例从 0.1 至 0.5 均匀采样，区间位于有效内容的起始、中部、末端或随机位置。遮挡只作用于原来可观测的位置，文本的首尾结构标记不参与遮挡。这一构造使训练输入覆盖不同模态组合和局部连续缺失形式。

记加权交叉熵与 Smooth L1 强度损失之和为 $L_{\mathrm{task}}$，上标 $d$ 和 $c$ 分别表示缺失输入与完整输入。训练目标为

$$L=L_{\mathrm{task}}^{d}+0.3L_{\mathrm{task}}^{c}+0.1L_{\mathrm{rec}}+0.1L_{\mathrm{con}}. \tag{3}$$

$L_{\mathrm{rec}}$ 仅在人工遮挡位置比较缺失输入的潜在重建向量与完整输入的投影向量，后者停止梯度；$L_{\mathrm{con}}$ 对完整、缺失输入的类别概率施加均方误差，对情感强度施加 Smooth L1 约束。分类权重只依据训练集类别频数 $n_c$ 计算，为 $3395/(3n_c)$。式（3）的训练联合损失包含辅助任务，因此不应与单独用完整输入评估的任务损失混作同一指标。

## 4 训练设置与模型选择

使用 AdamW 优化，初始学习率为 $2\times10^{-4}$，权重衰减为 0.01，批量大小为 32，梯度范数上限为 1。第 6 轮起学习率乘以 0.25，降至 $5\times10^{-5}$。随机种子固定为 2028，最多训练 12 轮，早停耐心值为 4。每轮在验证集计算完整输入指标，以及固定随机位置下 TAV 三模态 30% 缺失指标，并用

$$S=0.5F_{1,\mathrm{valid}}^{c}+0.5F_{1,\mathrm{valid}}^{d}-0.05\mathrm{MAE}_{\mathrm{valid}}^{d} \tag{4}$$

选择检查点。最高分对应第 9 轮。测试集在模型确定后用于报告结果，不参与式（4）的比较。第 9 轮固定遮挡条件下，验证集 Accuracy 为 0.6497，Macro F1 为 0.6340，MAE 为 0.6199。这里是每轮使用同一随机规则的一次验证结果；后文的五次随机位置重复更适合描述缺失鲁棒性。

训练联合损失由第 1 轮的 2.3599 降至第 9 轮的 1.2020，并在第 12 轮降至 1.1112。验证集指标却不是逐轮单调改善：第 9 轮完整输入 Macro F1 为 0.6303，第 12 轮为 0.6198。训练继续改善而验证结果回落，提示后期泛化收益有限。分类边界的变化、验证样本量及类别分布、分类与回归联合优化，以及固定缺失位置的单次抽样，都可能使验证 F1 和 MAE 产生波动；这些是与观测一致的解释，现有图表不能单独判定各因素的贡献。

## 5 预测结果与缺失鲁棒性

### 5.1 完整输入下的总体结果

| 数据划分 | 样本数 | Accuracy | Macro F1 | MAE | Pearson $r$ |
| :--- | ---: | ---: | ---: | ---: | ---: |
| 训练集 | 3395 | 0.7493 | 0.7265 | 0.4802 | 0.8235 |
| 验证集 | 728 | 0.6484 | 0.6303 | 0.6205 | 0.6460 |
| 测试集 | 727 | 0.6795 | 0.6398 | 0.6435 | 0.6776 |

Accuracy 为三类预测正确比例；Macro F1 是三类 F1 的算术平均；MAE 是强度预测与真实强度的平均绝对误差。总体指标用完整输入计算。验证集与测试集的 Accuracy 差异不能直接解释为后者训练效果更好，两者是不同样本集合。

### 5.2 逐情感标签结果

下表的单标签 Accuracy 和 Macro F1 都按“该类对其他两类”的二分类口径计算，并非该类召回率；MAE 仅在真实标签属于该类的样本上计算。

| 划分 | 情感标签 | 样本数 | 一对其余 Accuracy | 一对其余 Macro F1 | 类内 MAE |
| :--- | :--- | ---: | ---: | ---: | ---: |
| 训练 | Negative | 967 | 0.8872 | 0.8652 | 0.5727 |
| 训练 | Neutral | 758 | 0.8062 | 0.7242 | 0.3630 |
| 训练 | Positive | 1670 | 0.8053 | 0.8048 | 0.4797 |
| 验证 | Negative | 206 | 0.8077 | 0.7664 | 0.7988 |
| 验证 | Neutral | 184 | 0.7445 | 0.6665 | 0.5022 |
| 验证 | Positive | 338 | 0.7445 | 0.7421 | 0.5762 |
| 测试 | Negative | 207 | 0.8336 | 0.8015 | 0.8480 |
| 测试 | Neutral | 158 | 0.7607 | 0.6433 | 0.5188 |
| 测试 | Positive | 362 | 0.7648 | 0.7646 | 0.5810 |

测试集 Neutral 的类别召回率另按真正例除以该类真实样本数计算，为 $68/158=0.4304$。测试混淆矩阵中，55 条真实 Neutral 样本被预测为 Positive，说明中性与正向边界仍是主要分类难点之一。Negative 的类内强度 MAE 在验证、测试集分别为 0.7988、0.8480，也提示其强度预测误差较大。

### 5.3 局部缺失实验

在验证集上，对 7 种模态组合分别设置 10%、20%、30%、40%、50% 目标缺失比例，并改变缺失区间位置。随机位置每种条件重复五次；图中的误差条表示这五次的标准差。目标比例与实际比例不必相等，因为遮挡只在各样本原本有效的位置进行，文本结构标记也被排除。完整输入验证集 Macro F1 为 0.6303，MAE 为 0.6205。

| 缺失模态及目标比例 | 平均实际比例 | Macro F1 | 相对完整输入 | MAE | 相对完整输入 |
| :--- | ---: | ---: | ---: | ---: | ---: |
| T，30% | 26.9% | 0.6217 | −0.0086 | 0.6188 | −0.0017 |
| A，30% | 29.9% | 0.6264 | −0.0039 | 0.6246 | +0.0041 |
| V，30% | 29.1% | 0.6283 | −0.0020 | 0.6235 | +0.0030 |
| TAV，30% | 28.8% | 0.6210 | −0.0093 | 0.6263 | +0.0059 |
| TAV，50% | 47.9% | 0.6053 | −0.0250 | 0.6302 | +0.0097 |
| AV，50% | 49.7% | 0.6222 | −0.0081 | 0.6430 | +0.0225 |

这些结果描述该固定模型在给定验证集和遮挡机制下的表现，不是跨模型或跨数据集的稳健性保证。例如 AV 同时缺失约 50% 时，分类 F1 下降有限而强度 MAE 增加 0.0225，说明分类与强度预测对缺失的敏感性并不完全相同。若部分配对 Bootstrap 置信区间跨越零，应把相应差值视为未充分证明的方向性变化。

### 5.4 附件 3 推断

用第 9 轮权重对附件 3 的 30 条对齐样本预测，得到 Negative 6 条、Neutral 8 条、Positive 16 条；预测强度均值为 0.4080。附件 3 没有情感类别和强度真值，因而不能报告其 Accuracy、F1、MAE，也不能仅凭模型输出概率声称概率已经校准。

## 6 最终交付图像逐图解释

本节所述图均位于 [`release/seed2028/charts`](release/seed2028/charts)，其中图 1 至图 14 对应最终 seed 2028 模型；图 A1 明确属于 seed 2026、dropout 0.2 的历史架构消融。以下说明同时适用于 `outputs/deliverable_seed2028/charts` 中与之逐文件相同的 PNG 副本。

**[图 1 模型数据流](release/seed2028/charts/01_model_flow.png)。** 展示三个输入模态、质量与可用掩码、单模态时序编码、跨模态重建、等权融合、时间池化及两个预测头的先后关系。它是结构示意图，不承载准确率数值。

**[图 2 训练曲线](release/seed2028/charts/02_training_curves.png)。** 左侧为训练联合损失及评估任务损失，其他面板比较训练集完整输入、验证集完整输入与验证集 TAV 30% 固定缺失输入的 Accuracy、Macro F1 和 MAE。学习率在第 6 轮改变，竖向紫色虚线标出这一时间点；第 9 轮的选中权重由另一条竖线指示。训练损失持续下降不意味着验证 F1 或 MAE 必须单调变化。

**[图 2b 完整与缺失验证曲线](release/seed2028/charts/02b_missing_training_curves.png)。** 将完整输入和 TAV 30% 缺失输入的验证 Accuracy、Macro F1、MAE 直接放在同一组逐轮坐标中，便于判断缺失造成的差距是否随训练改变。图中的缺失验证是一次固定遮挡实现，不是每轮五次随机遮挡均值；训练联合损失也不是“缺失验证损失”。

**[图 3 三个划分的总体指标](release/seed2028/charts/03_split_metrics.png)。** 并列展示训练、验证、测试集的 Accuracy、Macro F1、MAE 和 Pearson 相关系数。验证及测试上的置信区间来自样本 Bootstrap，表示同一测试设置下指标的抽样不确定性，不代表三组重新训练的结果范围。

**[图 4 逐标签指标](release/seed2028/charts/04_label_metrics.png)。** 分别显示 Negative、Neutral、Positive 在三个划分上的一对其余 Accuracy、二分类 Macro F1 和真实该类样本的强度 MAE。阅读时应与类别召回率区分，图中每类的 Accuracy 包含“其余两类被正确排除”的样本。

**[图 5 混淆矩阵](release/seed2028/charts/05_confusion_matrices.png)。** 给出三个数据划分的真实类别与预测类别交叉计数，并以行归一化比例辅助比较类别召回。测试集中性情感有 68 条判对，55 条误判为 Positive，显示其类别边界相对模糊。

**[图 6 情感强度散点](release/seed2028/charts/06_regression_scatter.png)。** 横轴为真实强度，纵轴为预测强度，对角线表示完全一致。散点离对角线越远，单样本绝对误差越大；图像可用来识别极端强度处的预测收缩，但仅凭散点不能判断其成因。

**[图 7 验证集误差切片](release/seed2028/charts/07_validation_error_slices.png)。** 按文本长度及原始视觉有效比例划分验证样本，比较各组预测误差。它用于定位可能的弱势子群，组间差异是描述性关联，不能据此认定文本长度或视觉质量单独导致错误。

**[图 8 缺失比例曲线](release/seed2028/charts/08_missing_rate_curves.png)。** 横轴为目标缺失比例，分别绘出七种缺失模态组合的 Macro F1 和 MAE；随机位置的点采用五次重复的均值与标准差，虚线为完整输入基线。它回答“缺失比例增加时总体指标如何变化”。

**[图 9 缺失比例热图](release/seed2028/charts/09_missing_rate_heatmaps.png)。** 以七种模态组合和五个目标缺失比例构成网格，颜色显示相对完整输入的 Macro F1 或 MAE 变化。两种指标的方向不同：F1 下降通常是不利变化，MAE 上升通常是不利变化；不能把两个面板的相同颜色直接解释为同等程度的损失。

**[图 10 缺失位置热图](release/seed2028/charts/10_missing_position_heatmaps.png)。** 汇总起始、中部、末端及随机位置的缺失影响，图中对多个缺失比例求平均。它用于比较缺失发生在序列不同位置时的敏感性，不表示任意单一比例下的精确变化。

**[图 11 三模态 30% 缺失的配对区间](release/seed2028/charts/11_missing_30pct_paired_ci.png)。** 在同一验证样本上比较完整输入和 TAV 30% 缺失输入，点为指标差值，横向区间为配对样本 Bootstrap 的 95% 区间。区间若跨过零，不能据此断言该设置下存在稳定的非零差异；五次随机位置重复是不同遮挡实现，并非五个独立训练模型。

**[图 12 附件 3 逐样本预测](release/seed2028/charts/12_attachment3_predictions.png)。** 对 30 条无标签样本分别展示三类输出概率、预测强度及输入原有缺失比例。概率来自 softmax，图中未进行概率校准或与真值核对。

**[图 13 附件 3 预测分布](release/seed2028/charts/13_attachment3_distribution.png)。** 汇总预测类别数及预测强度的分布。Positive 的 16 条是模型预测数，不是该附件真实正向样本数。

**[图 A1 历史架构消融](release/seed2028/charts/A1_historical_architecture_ablation.png)。** 比较不同结构变体在完整输入及 30% 缺失下的分类与回归表现。原实验使用随机种子 2026、dropout 0.2，不能把图中柱高当作最终 seed 2028 模型的逐项消融结果；它只提供建模取舍的历史参考。

## 7 `problem2` 目录其他图像的来源与解释

`outputs` 保存了调参及重测过程。它们的种子、dropout、隐藏维度或是否使用参数滑动平均并不相同。下列每个 PNG 均在此列出，以避免将历史图像误作最终结果。与最终交付完全相同的副本在文末说明。

### 7.1 早期固定门控模型与第 6 轮降学习率实验

**[`outputs/final/training_curves.png`](outputs/final/training_curves.png)**：初始固定门控模型的训练联合损失、完整验证和缺失验证曲线，使用 seed 2026、dropout 0.2、未在第 6 轮降学习率；该模型选中第 5 轮。

**[`outputs/final/training_curves_with_train.png`](outputs/final/training_curves_with_train.png)**：同一早期模型的曲线增加了训练集完整输入指标，用来显示训练与验证间的差距，并不是 seed 2028 的最终训练轨迹。

**[`outputs/final/label_metrics_chart.png`](outputs/final/label_metrics_chart.png)**：早期模型在训练、验证、测试上的逐标签一对其余指标与强度 MAE；统计口径与图 4 相同，但权重不同。

**[`outputs/final/valid_robustness_curve.png`](outputs/final/valid_robustness_curve.png)**：早期模型在不同模态组合、遮挡比例与位置下的验证表现，供该轮方法调试使用。

**[`outputs/lr_drop_epoch6/report/training_curves.png`](outputs/lr_drop_epoch6/report/training_curves.png)**：seed 2026、dropout 0.2 的固定门控模型在第 6 轮降低学习率后的损失及指标曲线，用来观察降学习率后的走势；其选中轮次为第 9 轮。

**[`outputs/lr_drop_epoch6/report/label_metrics.png`](outputs/lr_drop_epoch6/report/label_metrics.png)**：上述降学习率模型在各标签上的分类和回归误差，与最终图 4 的图形含义相同，数值来源不同。

**[`outputs/lr_drop_epoch6/report/valid_robustness_curve.png`](outputs/lr_drop_epoch6/report/valid_robustness_curve.png)**：按遮挡发生位置分面的验证 Macro F1 曲线，用于观察起始、中部、末端、随机遮挡的差别。

**[`outputs/lr_drop_epoch6/report/modality_rate_curves.png`](outputs/lr_drop_epoch6/report/modality_rate_curves.png)**：七种模态组合随遮挡比例变化的平均 Macro F1 与 MAE，是最终图 8 的早期模型版本。

**[`outputs/lr_drop_epoch6/report/modality_rate_heatmaps.png`](outputs/lr_drop_epoch6/report/modality_rate_heatmaps.png)**：将上述模态组合与比例实验绘为相对完整输入的指标变化热图，是最终图 9 的早期模型版本。

**[`outputs/lr_drop_epoch6/report/architecture_ablations_original_lr.png`](outputs/lr_drop_epoch6/report/architecture_ablations_original_lr.png)**：原始恒定学习率条件下的架构消融。它与最终交付的历史消融图均属早期结构比较，不属于最终 seed 2028 的同条件消融。

**[`outputs/lr_drop_epoch6/report/attachment3_predictions_chart.png`](outputs/lr_drop_epoch6/report/attachment3_predictions_chart.png)**：上述早期模型对附件 3 的逐样本类别概率、强度及缺失信息展示；由于无标签，不能以图中预测与最终图 12 的差别判断哪一版更准确。

**[`outputs/valid_robustness_curve.png`](outputs/valid_robustness_curve.png)**：更早的初始模型验证鲁棒性曲线。其关联训练配置为 seed 2026、dropout 0.2、动态门控版本；它用于与后来固定门控方案比较，不应引用为最终模型的缺失性能。

### 7.2 隐藏维度、参数平均与 dropout 比较

**[`outputs/hidden96_lr_drop/report/seed2026_training_curves.png`](outputs/hidden96_lr_drop/report/seed2026_training_curves.png)**：隐藏维度 96、seed 2026 模型的训练损失及完整训练、验证指标曲线，观察缩小模型后的收敛情况。

**[`outputs/hidden96_lr_drop/report/three_seed_validation_comparison.png`](outputs/hidden96_lr_drop/report/three_seed_validation_comparison.png)**：按 seed 2026、2027、2028 比较 96 维与 128 维模型的逐轮验证曲线；每条线对应单次训练，不能当作置信区间。

**[`outputs/hidden96_lr_drop/report/selected_model_comparison.png`](outputs/hidden96_lr_drop/report/selected_model_comparison.png)**：比较上述两种隐藏维度在各随机种子下选中权重的验证指标。三种子平均值中，96 维的验证 Accuracy、Macro F1 分别为 0.6264、0.6175，128 维基线分别为 0.6337、0.6217；这说明缩小模型在该组实验中没有提升分类均值。

**[`outputs/ema_lr_drop/report/seed2026_training_curves.png`](outputs/ema_lr_drop/report/seed2026_training_curves.png)**：seed 2026 下，原始参数与指数滑动平均参数的训练或验证曲线，用于观察平均参数是否缓和逐轮波动。

**[`outputs/ema_lr_drop/report/three_seed_validation_curves.png`](outputs/ema_lr_drop/report/three_seed_validation_curves.png)**：三个种子的原始参数与 EMA 参数逐轮验证轨迹，实线与虚线区分两种权重读取方式；它不是最终模型的逐轮曲线。

**[`outputs/ema_lr_drop/report/selected_model_comparison.png`](outputs/ema_lr_drop/report/selected_model_comparison.png)**：比较三个种子下两种参数版本的选中模型指标。EMA 实验的验证 Macro F1 均值约 0.6171，原始参数版本约 0.6217，因此平滑曲线本身不代表最终分类改善。

**[`outputs/dropout03_lr_drop/report/seed2026_training_curves.png`](outputs/dropout03_lr_drop/report/seed2026_training_curves.png)**：dropout 0.3、seed 2026 的降学习率训练轨迹，用于观察增加 dropout 后该种子的拟合过程。

**[`outputs/dropout03_lr_drop/report/seed2028_training_curves.png`](outputs/dropout03_lr_drop/report/seed2028_training_curves.png)**：dropout 0.3、seed 2028 的重跑轨迹，和最终图 2 对应同一最终训练配置；图形布局和统计展示范围不同。

**[`outputs/dropout03_lr_drop/report/three_seed_validation_curves.png`](outputs/dropout03_lr_drop/report/three_seed_validation_curves.png)**：三个种子下 dropout 0.2 与 0.3 的逐轮验证指标比较；区分训练配置后再阅读各曲线。

**[`outputs/dropout03_lr_drop/report/selected_model_comparison.png`](outputs/dropout03_lr_drop/report/selected_model_comparison.png)**：三个种子选中权重的 dropout 0.2 与 0.3 验证指标比较。dropout 0.3 的平均 MAE 为 0.6123，dropout 0.2 为 0.6322；两者平均 Macro F1 均约 0.6217。最终采用的是 dropout 0.3 实验中的 seed 2028 权重，而不是把三个种子的预测平均。

### 7.3 附件 3 的另一种对齐比较及最终模型补图

**[`outputs/attachment3_both_versions/attachment3_both_versions.png`](outputs/attachment3_both_versions/attachment3_both_versions.png)**：对附件 3 的对齐版与非对齐版分别使用相应模型推断，比较 30 条样本的类别与强度。两版类别一致 22 条，强度预测绝对差的均值为 0.3864。模型和输入表示同时变化，且没有真值，因此该图只说明预测一致程度，不能用于判定哪种对齐方式更准确。

**[`outputs/deliverable_seed2028/valid_robustness_curve.png`](outputs/deliverable_seed2028/valid_robustness_curve.png)**：最终 seed 2028 权重的验证鲁棒性补图，按遮挡位置比较多种模态组合随缺失比例变化的曲线，可与最终图 8 至图 10 相互参照。

**[`outputs/retest_seed2028/valid_robustness_curve.png`](outputs/retest_seed2028/valid_robustness_curve.png)**：重测目录中的同一补图，与上一张 PNG 内容相同，不构成独立实验结果。

`outputs/deliverable_seed2028/charts` 的 15 张 PNG 与 `release/seed2028/charts` 的 15 张 PNG 逐一相同，其解释均见第 6 节。整个 `problem2` 目录共找到 55 个 PNG 文件，其中重复副本合并后有 39 种不同的图像内容。PDF 是相应图像的矢量版或合订版，读图含义相同，不另作实验计数。

## 8 讨论与结论

最终模型通过三种掩码、训练集统计量标准化、连续块遮挡和跨模态潜在重建，处理了局部缺失及特征数值异常。完整输入的测试集 Accuracy 为 0.6795，Macro F1 为 0.6398，MAE 为 0.6435。验证集随机位置三模态遮挡约 30% 时，五次重复平均 Macro F1 比完整输入低 0.0093，MAE 高 0.0059。下降幅度与遮挡模态、位置及预测任务有关，不能用单一指标概括所有缺失场景。

方法仍有两项明确局限。其一，Neutral 的测试集召回率仅为 0.4304，说明一对其余 Accuracy 不能掩盖类别内部的漏判。其二，附件 3 无真值，所有类别和强度结果都只是待核验预测。若后续需要宣称模型对真实缺失场景稳定，仍应在带真值且缺失机制已知的外部数据上重复评估，并分别报告分类、强度和不同模态组合的误差。

## 附录 复现依据

预处理实现见 [`data.py`](data.py)、[`vendor/mmer_preprocess/masks.py`](vendor/mmer_preprocess/masks.py)、[`vendor/mmer_preprocess/stats.py`](vendor/mmer_preprocess/stats.py)；模型结构见 [`model.py`](model.py)；训练、遮挡与评价见 [`experiment.py`](experiment.py)。最终配置、逐轮指标、逐标签结果和缺失实验原始表分别见 [`training_config.json`](release/seed2028/training_config.json)、[`seed2028_epoch_metrics.csv`](release/seed2028/seed2028_epoch_metrics.csv)、[`label_metrics.csv`](release/seed2028/label_metrics.csv)、[`valid_robustness.csv`](release/seed2028/valid_robustness.csv)。最终模型权重与附件 3 逐条预测均保存在 [`release/seed2028`](release/seed2028)。
